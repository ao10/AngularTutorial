// This is an interface which tells Angular what kind of data is on the backend that we need to display on the frontend
export interface User {
    userID: Number,
    id: Number,
    title: String,
    body: String,
}